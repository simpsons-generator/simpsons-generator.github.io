var GHPATH = 'https://simpsons-generator.github.io/generator.html';
var APP_PREFIX = 'simp.gen';
var VERSION = 'version_001';
var URLS = [    
  `${GHPATH}/`,
  `${GHPATH}/index.html`,
  `${GHPATH}/sw.js`,
  `${GHPATH}/manifest.webmanifest`,
  `${GHPATH}/images.png`,
  `${GHPATH}/style.css`,
  `${GHPATH}/script.js`,
  `${GHPATH}/generator.html`,
]
